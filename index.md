# 量潮数据工程意图

量潮数据工程意图记录的是量潮数据云怎样把数据工程能力做成一条可托管、可计量、可审计、可复用的执行线。它不是单纯讲项目交付，而是讲数据从进入系统开始，到采集、清洗、治理、计算、交付和复盘结束为止，整条链路怎么被统一成一次 run。

## 适用范围

- 需要托管执行的数据任务
- 需要多租户、配额、权限和审计的数据平台
- 需要 CLI 获客、服务端履约的产品
- 需要把 AI、模板、报告和执行结果统一管理的场景

## 工作原则

- 先有 run，再谈功能拆分。
- 所有能力都围绕一次执行来组织。
- AI 只是执行资源，不是第二个计价器。
- 结果必须可回放、可追溯、可交付。
- 对外表达尽量简单，对内治理要足够细。

## 角色分工

- 负责人负责确认目标、范围和验收口径。
- 数据工程负责设计数据流、执行链路和异常处理。
- 平台负责提供运行环境、权限、计量和审计能力。
- AI 负责补齐蓝图、整理步骤、生成报告草稿。
- 审计负责核对事实源、成本口径和证据链。

## 关键对象

| 对象 | 含义 |
|------|------|
| `blueprint` | 描述一次数据工程要做什么 |
| `run` | 一次可计量的执行 |
| `job` | run 运行时的状态记录 |
| `resource` | 计算、存储、AI 调用等成本项 |
| `report` | 对外交付的结果 |
| `audit log` | 审计证据和追踪记录 |

## 生命周期

### 定义

先把问题边界定清楚：输入是什么，输出是什么，谁来执行，谁来验收。

### 设计

把数据流、权限、资源档位、校验规则和失败回退方式设计出来。

### 执行

把一次数据工程变成一条明确的 run，能跑、能停、能看状态。

### 校验

确认数据是否到位、格式是否正确、结果是否符合约定。

### 交付

把报告、结果集、日志和证据一起交出去，而不是只交一个页面。

### 复盘

记录这次执行的成本、耗时、异常和可复用部分，作为下一次优化依据。

## 商业模式

> 托管执行 + 按次计量：跑一次收一次钱。其他所有功能都是挂在这个主干上的 resource。

服务端提供托管执行，按次计量收费。这是最核心的收费方式，因为：

- 远程统计执行、AI 生成、报告生成、模板实例化，本质上都是一次 run 的不同 resource
- 运行成本可以统一落到一次执行上，计费口径简单
- 收入和使用强绑定，客户跑了多少，就付了多少

CLI 负责获客，服务端负责履约和收费。客户可以先用本地工具理解和设计，再把真正的执行放到服务端完成。

```text
单价 = 执行时长 × 资源档位系数 × 是否可抢占
```

AI 功能不单独开第二套 token 计费体系，而是作为 run 的一种 resource 处理。这样做的好处是，AI 成本下降时，毛利空间反而会被放大，而不是被单独的 token 价格战拖住。

参考对象可以看成是 Modal 和 Posit Cloud 这类“把执行环境托管出来并按使用计量”的产品，但不照抄它们的订阅复杂度。

## 交付标准

- 有明确的 run id
- 有输入和输出边界
- 有 started_at 和 finished_at
- 有结果、报告和证据链
- 有可追溯的成本口径
- 有必要时的回退路径

## 第一里程碑

把 `POST /blueprints/{name}/runs` 升级成一个多租户、带沙箱、可计量的执行入口。

先做单档单价、按秒计量和 job 记录当账单依据，等有客户和真实使用数据之后，再加资源档位和折扣，不做 DBU 式的复杂计价体系。
